# Getting Started

Este documento explica como preparar o ambiente e rodar o projeto localmente.

- Pré-requisitos: nvm, Node 20 LTS (lts/iron), npm
- Instalar dependências: `npm install`
- Rodar em dev: `npm run dev` (abre em http://localhost:3000)

Veja `README.md` para instruções rápidas.
