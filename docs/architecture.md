# Arquitetura

- Next.js 16 (Turbopack)
- React 19
- Pastas principais: `pages`/`app` (dependendo da versão do Next), `public`, `src`.

Notas:
- O projeto usa `.nvmrc` para indicar a versão Node LTS (recomendado usar `nvm`).
- Atualize a seção se sua estrutura real divergir.
